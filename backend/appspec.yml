version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/backend
hooks:
  AfterInstall:
    - location: scripts/install.sh
      timeout: 300
      runas: ubuntu
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
      runas: ubuntu
