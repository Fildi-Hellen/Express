version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing source NPM dependencies...
      - cd Expressud/expressud
      - npm install --legacy-peer-deps
  build:
    commands:
      - echo Build started 
      - ng build --prod --output-path=dist/expressud # adjust per project